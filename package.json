{
  "name": "festival-teatro",
  "version": "1.0.0",
  "description": "Festival Internacional de Teatro de Guaranésia",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd admin-panel && npm run dev\"",
    "build": "npm run build --prefix frontend && npm run build --prefix admin-panel",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix admin-panel"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
